import { Grid, styled } from "@mui/material";

const MainContainer = styled(Grid)({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  minHeight: '100vh',
});

export {
  MainContainer,
};
