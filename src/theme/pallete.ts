import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#137fcb',
      main: '#1565c0',
      dark: '#093f65',
    },
    secondary: {
      main: '#fff',
    },
    error: {
      main: '#ff0000',
    },
  },
});
